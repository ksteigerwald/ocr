### Business Card Parser

To execute, clone the repository and inside the terminal enter:

node app.js file=[path/to/your/file]

Example files are hosted inside the cards directory:

node app.js file=./cards/one.txt
